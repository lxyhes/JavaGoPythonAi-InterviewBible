export interface QAItem {
  question: string
  tags: { text: string; type: 'must' | 'frequent' | 'important' }[]
  answer: string
}

export const javaQA: QAItem[] = [
  {
    question: 'Java 中的集合类有哪些？List、Set、Map 的区别？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `**Collection 接口：**
- **List**：有序可重复（ArrayList、LinkedList、Vector）
- **Set**：无序不可重复（HashSet、TreeSet、LinkedHashSet）

**Map 接口：**
- 键值对存储（HashMap、TreeMap、LinkedHashMap、ConcurrentHashMap）

**区别：** List 允许重复保持顺序；Set 不允许重复；Map 存储键值对`,
  },
  {
    question: 'HashMap 的底层原理是什么？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `**底层结构：** 数组 + 链表 + 红黑树（JDK1.8）

**JDK1.7 vs 1.8：**
- 1.7 使用头插法可能产生死循环
- 1.8 使用尾插法，链表长度≥8 转为红黑树

**put 流程：**
计算 hash → 计算索引 → 无冲突直接放入 → 有冲突用链表/红黑树 → 超过阈值 0.75 扩容`,
  },
  {
    question: 'synchronized 和 ReentrantLock 的区别？',
    tags: [{ text: '高频', type: 'frequent' }],
    answer: `- synchronized 是 JVM 层面的，自动获取/释放锁
- ReentrantLock 是 API 层面的，需要手动 lock/unlock
- ReentrantLock 支持可中断、超时获取、公平锁
- ReentrantLock 可以绑定多个 Condition`,
  },
  {
    question: 'Java 线程池的核心参数有哪些？',
    tags: [{ text: '必问', type: 'must' }],
    answer: `1. **corePoolSize**：核心线程数
2. **maximumPoolSize**：最大线程数
3. **keepAliveTime**：非核心线程存活时间
4. **workQueue**：任务等待队列
5. **threadFactory**：线程工厂
6. **handler**：拒绝策略

**拒绝策略：** AbortPolicy（抛异常）、CallerRunsPolicy（调用者执行）、DiscardPolicy（静默丢弃）、DiscardOldestPolicy（丢弃最老）`,
  },
  {
    question: 'JVM 的内存结构是怎样的？',
    tags: [{ text: '重要', type: 'important' }],
    answer: `**运行时数据区：**
- **堆（Heap）**：存放对象实例，垃圾回收主要区域
- **虚拟机栈**：存放局部变量、操作数栈
- **本地方法栈**：Native 方法服务
- **方法区（元空间）**：类信息、常量、静态变量
- **程序计数器**：当前线程执行的字节码行号`,
  },
]

export const microservicesQA: QAItem[] = [
  {
    question: '微服务架构的优缺点是什么？',
    tags: [{ text: '必问', type: 'must' }],
    answer: `## 通俗理解
想象你在经营一家餐厅：
- **单体架构**：一个大厨房，所有厨师在一起，一个厨师请假整个厨房受影响
- **微服务架构**：分成多个小厨房（中餐、西餐、甜点），各自独立，互不影响

微服务 = "把大系统拆成小服务，各自独立部署"

### 架构对比
\`\`\`
单体架构:                              微服务架构:
┌─────────────────────┐               ┌─────┐ ┌─────┐ ┌─────┐
│                     │               │用户 │ │订单 │ │库存 │
│  ┌───────────────┐  │               │服务 │ │服务 │ │服务 │
│  │   用户模块     │  │               └──┬──┘ └──┬──┘ └──┬──┘
│  ├───────────────┤  │                  │       │       │
│  │   订单模块     │  │                  └───────┼───────┘
│  ├───────────────┤  │                          │
│  │   库存模块     │  │                    ┌─────────┐
│  ├───────────────┤  │                    │  API网关 │
│  │   支付模块     │  │                    └────┬────┘
│  └───────────────┘  │                         │
│                     │                        客户端
│   一个代码库         │
│   一起部署           │
└─────────────────────┘
\`\`\`

## 详细对比
\`\`\`
┌──────────────┬──────────────────┬──────────────────┐
│    特性      │    单体架构       │   微服务架构      │
├──────────────┼──────────────────┼──────────────────┤
│  代码库      │    单一          │    多个独立       │
│  部署        │    整体部署      │    独立部署       │
│  扩展        │    整体扩展      │    按需扩展       │
│  技术栈      │    统一          │    异构可选       │
│  故障隔离    │    差            │    好            │
│  开发效率    │    初期快        │    长期快        │
│  运维复杂度  │    低            │    高            │
│  数据一致性  │    容易保证      │    需要处理      │
└──────────────┴──────────────────┴──────────────────┘
\`\`\`

## 微服务优缺点
\`\`\`
优点:
✓ 独立部署，互不影响
✓ 技术栈灵活，选择最适合的
✓ 团队自治，开发效率高
✓ 按需扩展，资源利用率高
✓ 故障隔离，单点故障不影响整体

缺点:
✗ 分布式复杂度增加
✗ 服务间通信开销
✗ 数据一致性难保证
✗ 运维成本高
✗ 需要完善的DevOps体系
\`\`\``
  },
  {
    question: '服务注册与发现的原理？',
    tags: [{ text: '高频', type: 'frequent' }],
    answer: `## 通俗理解
想象你去商场找餐厅：
- **没有注册中心**：你要记住每家餐厅的位置，新店开业你也不知道
- **有注册中心**：看商场导览图（注册中心），餐厅位置一目了然

服务注册与发现 = "服务的"通讯录"，让服务找到彼此"

### 架构原理
\`\`\`
┌─────────────────────────────────────────────────────────┐
│                   服务注册与发现流程                     │
│                                                         │
│  服务启动时:                                            │
│  ┌──────────┐                                          │
│  │  服务A   │ ──注册──> ┌──────────────┐               │
│  │ (IP:Port)│          │  注册中心     │               │
│  └──────────┘          │  (Nacos/     │               │
│                        │   Consul)    │               │
│  ┌──────────┐          └──────┬───────┘               │
│  │  服务B   │ ──注册────────>│                        │
│  │ (IP:Port)│                 │                        │
│  └──────────┘                 │                        │
│                               │                        │
│  服务调用时:                  │                        │
│  ┌──────────┐                 │                        │
│  │  服务A   │ ──查询服务B──>  │                        │
│  │          │ <─返回服务B地址─┘                        │
│  └────┬─────┘                                          │
│       │                                                 │
│       └────────直接调用────────> ┌──────────┐          │
│                                  │  服务B   │          │
│                                  └──────────┘          │
│                                                         │
└─────────────────────────────────────────────────────────┘
\`\`\`

## 心跳机制
\`\`\`
┌─────────────────────────────────────────────────────┐
│              健康检查与心跳                          │
├─────────────────────────────────────────────────────┤
│                                                     │
│  服务注册后需要定期发送心跳：                        │
│                                                     │
│  服务A ──心跳──> 注册中心                            │
│    │               │                                │
│  每5秒发送一次      │ 检测心跳超时                   │
│    │               │                                │
│  如果15秒没收到 ────┘ 标记服务不健康                  │
│                                                     │
│  如果30秒没收到 ────── 剔除服务                      │
│                                                     │
│  保护机制：                                          │
│  - 防止网络抖动导致误剔除                           │
│  - 支持优雅下线（主动注销）                         │
│                                                     │
└─────────────────────────────────────────────────────┘
\`\`\`

## 常见注册中心对比
\`\`\`
┌─────────────┬─────────────┬─────────────┬─────────────┐
│    特性     │    Nacos    │   Consul    │   Eureka    │
├─────────────┼─────────────┼─────────────┼─────────────┤
│  开发方     │   Alibaba   │  HashiCorp  │   Netflix   │
│  一致性协议 │   CP/AP     │    CP       │    AP       │
│  健康检查   │  心跳/HTTP  │  多种方式   │   心跳      │
│  多语言     │    好       │    好       │   Java为主  │
│  配置中心   │    支持     │   支持      │   不支持    │
│  性能       │    高       │   中        │   中        │
└─────────────┴─────────────┴─────────────┴─────────────┘
\`\`\`

**常用组件：** Eureka、Nacos、Consul、Zookeeper`,
  },
  {
    question: '分布式事务的解决方案？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `## 通俗理解
想象你要给朋友转账：
- **本地事务**：你自己数钱，要么成功要么失败
- **分布式事务**：你的钱在银行A，朋友的钱在银行B，需要两个银行协调

分布式事务 = "跨多个服务的操作，要保证一致性"

### 解决方案对比
\`\`\`
┌─────────────────────────────────────────────────────────┐
│              分布式事务解决方案                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 2PC (两阶段提交)                                    │
│     强一致性，同步阻塞，性能差                          │
│                                                         │
│  2. 3PC (三阶段提交)                                    │
│     改进2PC，减少阻塞，但复杂度高                       │
│                                                         │
│  3. TCC (Try-Confirm-Cancel)                            │
│     业务层实现，柔性事务，性能好                        │
│                                                         │
│  4. Saga 模式                                           │
│     长事务拆分，异步执行，补偿回滚                      │
│                                                         │
│  5. 本地消息表                                          │
│     最终一致性，可靠消息投递                            │
│                                                         │
│  6. MQ 事务消息                                         │
│     基于消息队列，异步解耦                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
\`\`\`

## TCC 详解
\`\`\`
TCC = Try - Confirm - Cancel

下单扣减库存示例:

Try 阶段（预留资源）:
┌──────────┐     ┌──────────┐     ┌──────────┐
│  订单服务  │     │  库存服务  │     │  账户服务  │
│  创建订单  │────→│  预扣库存  │────→│  冻结金额  │
│ (待支付)  │     │ (不可售)  │     │ (不可用)  │
└──────────┘     └──────────┘     └──────────┘

Confirm 阶段（确认执行）:
支付成功，确认所有操作
┌──────────┐     ┌──────────┐     ┌──────────┐
│  订单状态  │     │  库存扣减  │     │  金额扣除  │
│  更新为已支付  │     │  正式扣减  │     │  正式扣除  │
└──────────┘     └──────────┘     └──────────┘

Cancel 阶段（取消回滚）:
支付失败，释放预留资源
┌──────────┐     ┌──────────┐     ┌──────────┐
│  取消订单  │     │  释放库存  │     │  解冻金额  │
└──────────┘     └──────────┘     └──────────┘

特点:
- 业务层实现，需要写三套逻辑
- 性能较好，无全局锁
-  Confirm/Cancel 需要幂等
\`\`\`

## Saga 模式
\`\`\`
Saga = 长事务拆分成多个本地事务

订单流程示例:

正常流程:
T1: 创建订单 ──→ T2: 扣减库存 ──→ T3: 扣减余额 ──→ T4: 发送通知
    成功           成功            成功            成功

异常回滚:
T1: 创建订单 ──→ T2: 扣减库存 ──→ T3: 扣减余额 ❌
    成功           成功            余额不足!
                                    ↓
                              触发补偿
                                    ↓
T3补偿: 恢复余额 ←── T2补偿: 恢复库存 ←── T1补偿: 取消订单

两种实现方式:
1. 编排式 (Choreography): 每个服务完成触发下一个
2. 协调式 (Orchestration): 中央协调器统一调度

适用场景:
- 长业务流程
- 需要异步执行
- 可以接受最终一致性
\`\`\`

## 选择建议
\`\`\`
┌─────────────────────────────────────────────────────┐
│              分布式事务选择指南                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│  强一致性要求：                                      │
│  - 金融转账 → 2PC/TCC                               │
│                                                     │
│  最终一致性可接受：                                  │
│  - 电商下单 → Saga / 本地消息表                     │
│                                                     │
│  高并发场景：                                        │
│  - 秒杀活动 → TCC / 乐观锁                          │
│                                                     │
│  异步解耦：                                          │
│  - 通知发送 → MQ 事务消息                           │
│                                                     │
└─────────────────────────────────────────────────────┘
\`\`\`

**Seata：** Alibaba 开源的分布式事务解决方案，支持 AT、TCC、Saga、XA 模式`,
  },
  {
    question: '什么是熔断、限流、降级？',
    tags: [{ text: '必问', type: 'must' }],
    answer: `## 通俗理解
想象你在管理一家热门餐厅：
- **熔断**：厨房着火了，暂时不接新客人，等火灭了再说
- **限流**：餐厅满了，门口放排队号，控制进场人数
- **降级**：太忙了，暂停提供复杂菜品，只做简单的

这三者是微服务的"自我保护"机制

### 熔断 (Circuit Breaker)
\`\`\`
┌─────────────────────────────────────────────────────────┐
│                   熔断器状态流转                         │
│                                                         │
│   ┌──────────────┐    失败率>阈值     ┌──────────────┐ │
│   │   关闭状态    │ ─────────────────> │   打开状态    │ │
│   │  (正常通行)   │                    │  (拒绝请求)   │ │
│   └──────┬───────┘                    └──────┬───────┘ │
│          │                                    │         │
│          │ 请求成功                             │ 超时时间 │
│          │                                    │ 后尝试   │
│          │                                    ↓         │
│   ┌──────┴───────┐    失败          ┌──────────────┐   │
│   │   半开状态    │ <──────────────── │   打开状态    │   │
│   │  (试探请求)   │                    │   (继续)     │   │
│   └──────────────┘                    └──────────────┘   │
│                                                         │
│  关闭: 正常处理请求                                     │
│  打开: 快速失败，不调用下游服务                         │
│  半开: 放少量请求试探，成功则关闭，失败则打开           │
│                                                         │
└─────────────────────────────────────────────────────────┘
\`\`\`

### 限流 (Rate Limiting)
\`\`\`
┌─────────────────────────────────────────────────────────┐
│                   限流算法对比                           │
│                                                         │
│  1. 计数器算法                                          │
│     每秒限制100请求                                     │
│     计数器: 0 → 50 → 100 → 拒绝 → 0(下一秒)            │
│     缺点: 临界问题（1秒末和下一秒初可能瞬间200请求）    │
│                                                         │
│  2. 滑动窗口算法                                        │
│     把时间分成多个小窗口                                │
│     [0.8s][0.9s][1.0s][1.1s][1.2s]                     │
│     统计最近1秒内的请求总数                             │
│     优点: 平滑，无临界问题                              │
│                                                         │
│  3. 令牌桶算法                                          │
│     以固定速率往桶里放令牌                              │
│     请求来了拿令牌，拿到就执行，拿不到就限流            │
│     优点: 允许突发流量，平滑限流                        │
│                                                         │
│  4. 漏桶算法                                            │
│     请求像水一样流入漏桶                                │
│     以固定速率流出（处理）                              │
│     特点: 严格匀速，无突发                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
\`\`\`

### 降级 (Degradation)
\`\`\`
正常流程:
用户 ──查询商品详情──> 商品服务 ──查询库存──> 库存服务
                              ──查询评价──> 评价服务
                              ──查询推荐──> 推荐服务

降级后:
用户 ──查询商品详情──> 商品服务
                              ──查询库存──> 返回缓存数据
                              ──查询评价──> 返回空列表
                              ──查询推荐──> 返回默认推荐

降级方式：
- 返回默认值/缓存数据
- 关闭非核心功能
- 简化业务流程
\`\`\`

## 三者关系
\`\`\`
限流: 预防问题，控制入口流量
      "餐厅限流，控制进场人数"

熔断: 发现问题，阻断故障传播
      "厨房着火，暂停接单"

降级: 应对问题，保证核心功能
      "太忙了，只做简单菜"

使用顺序: 限流 → 熔断 → 降级
\`\`\`

**常用组件：** Sentinel、Hystrix、Resilience4j`,
  },
  {
    question: '微服务间的通信方式有哪些？如何选择？',
    tags: [{ text: '高频', type: 'frequent' }],
    answer: `## 通俗理解
想象你在公司不同部门之间沟通：
- **同步调用**：直接打电话，立刻得到回复（HTTP/RPC）
- **异步消息**：发邮件，对方有空再回复（消息队列）
- **事件驱动**：发公告，感兴趣的人自己来看（事件总线）

## 通信方式对比
\`\`\`
同步通信 (REST/gRPC):
服务A ──HTTP/RPC请求──> 服务B
服务A <──响应结果───── 服务B
特点: 实时、强一致性、耦合度高
适用: 查询操作、需要立即返回结果

异步通信 (消息队列):
服务A ──发送消息──> [消息队列] ──消费消息──> 服务B
服务A 继续执行，不等待
特点: 解耦、削峰填谷、最终一致性
适用: 订单处理、通知发送、耗时操作

事件驱动 (Event-driven):
服务A ──发布事件──> 事件总线
                         │
           ┌─────────────┼─────────────┐
           ↓             ↓             ↓
        服务B          服务C          服务D
       (订阅者)       (订阅者)       (订阅者)
特点: 完全解耦、广播、响应式
适用: 状态变更通知、多服务联动
\`\`\`

## 选择指南
\`\`\`
使用同步调用 (REST/gRPC)：
✓ 需要立即返回结果
✓ 强一致性要求
✓ 简单查询操作

使用异步消息队列：
✓ 耗时操作，不需要立即响应
✓ 需要削峰填谷
✓ 服务间解耦

使用事件驱动：
✓ 状态变更需要通知多个服务
✓ 业务流程复杂，需要 Saga 模式
\`\`\`
`,
  },
]

export const backendProjectQA: QAItem[] = [
  {
    question: '如何设计一个高并发的秒杀系统？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `**前端优化:**
- 静态资源 CDN
- 秒杀按钮防抖
- 验证码限流

**服务端优化:**
- Nginx 限流
- Redis 预减库存
- 异步下单（MQ）
- 数据库优化（分库分表）

**关键设计:**
- 库存预热到 Redis
- 异步处理订单
- 限流熔断
- 防超卖（Redis Lua 脚本）`,
  },
  {
    question: '项目中如何实现权限控制？',
    tags: [{ text: '高频', type: 'frequent' }],
    answer: `**RBAC 模型（基于角色的访问控制）：**
- 用户-角色-权限三层关系
- 角色继承
- 权限粒度控制

**实现方式：**
- Spring Security
- JWT Token
- OAuth2.0
- 注解权限控制（@PreAuthorize）`,
  },
]
