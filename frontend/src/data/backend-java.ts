import type { QAItem } from './backend'

export const javaBasicsQA: QAItem[] = [
  {
    question: 'Java 中 == 和 equals() 的区别？',
    tags: [{ text: '必问', type: 'must' }, { text: '基础', type: 'frequent' }],
    answer: `## 通俗理解

想象你比较两个人：
- **== 比较身份证**：只看是不是同一个人（内存地址）
- **equals 比较长相**：看两个人长得像不像（内容值）

### 对比图

      \`\`\`mermaid
      graph TD
          subgraph S1 ["== 运算符"]
              direction TB
              A1["基本类型"] --> B1["比较【值】是否相同"]
              A2["引用类型"] --> B2["比较【内存地址】是否相同"]
              
              A1 --> ex1["int a=10 == int b=10<br/>(true)"]
              A2 --> ex2["'hello'(A) == 'hello'(B)<br/>(false)"]
          end
          
          subgraph S2 ["equals() 方法"]
              direction TB
              C1["默认实现"] --> D1["与 == 相同<br/>(比较地址)"]
              C2["String/包装类已重写"] --> D2["比较【内容值】是否相同"]
              
              C2 --> ex3["'hello'.equals('hello')<br/>(true)"]
          end
      \`\`\`
      
      ### 经典面试陷阱
      
      \`\`\`java
      // 陷阱1：new String 创建的是不同对象
      String s1 = new String("hello");
      String s2 = new String("hello");
      s1 == s2      // false ❌ 地址不同
      s1.equals(s2) // true  ✅ 内容相同
      
      // 陷阱2：字符串常量池复用
      String s3 = "hello";  // 放入常量池
      String s4 = "hello";  // 复用常量池
      s3 == s4      // true ✅ 同一对象
      
      // 内存图
      // 堆内存              字符串常量池
      // ┌─────────┐        ┌─────────┐
      // │ String  │        │ "hello" │ ← s3, s4 指向这里
      // │ 对象1   │← s1    └─────────┘
      // ├─────────┤
      // │ String  │← s2
      // │ 对象2   │
      // └─────────┘
      \`\`\`
    `
  },
  {
    question: 'String、StringBuilder、StringBuffer 的区别？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `## 通俗理解

想象你在写一篇文章：
- **String**：用钢笔写在纸上，写错了只能撕掉重写（不可变）
- **StringBuilder**：用铅笔写，可以擦改，但只有你一个人写（单线程）
- **StringBuffer**：用铅笔写，还能加锁，多人一起写也不怕（多线程安全）

### 对比表

| 特性 | String | StringBuilder | StringBuffer |
| :--- | :--- | :--- | :--- |
| **可变性** | ❌ 不可变 | ✅ 可变 | ✅ 可变 |
| **线程安全** | ✅ 安全 | ❌ 不安全 | ✅ 安全 |
| **性能** | 慢 | **最快** | 中等 |
| **使用场景** | 常量定义、少量操作 | 单线程频繁拼接 | 多线程频繁拼接 |

### String 不可变的好处

\`\`\`mermaid
graph LR
    A[String 不可变] --> B[字符串常量池]
    A --> C[线程安全]
    A --> D[Hash 稳定性]
    A --> E[安全传输]

    B --- B1["复用对象<br/>节省内存"]
    C --- C1["只读操作<br/>无需加锁"]
    D --- D1["hashCode 缓存<br/>作为 Map 的 Key"]
    E --- E1["防止网络/路径<br/>参数被篡改"]
\`\`\`

### 性能对比示例

\`\`\`java
// ❌ 低效：创建10000个String对象
String s = "";
for (int i = 0; i < 10000; i++) {
    s += i;  // 每次创建新对象！
}

// ✅ 高效：只用1个StringBuilder
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 10000; i++) {
    sb.append(i);  // 直接修改内部数组
}
String result = sb.toString();
\`\`\``
  },
  {
    question: 'Java 中的异常体系？Checked 和 Unchecked 异常的区别？',
    tags: [{ text: '必问', type: 'must' }],
    answer: `## 通俗理解

想象医院看病：
- **Error**：绝症，医生也治不了（系统级错误）
- **Exception**：普通病，可以治疗
  - **Checked**：必须治疗的病，不治不让走（编译期检查）
  - **Unchecked**：小感冒，可以不治，但可能恶化（运行时）

### 异常体系图

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                      Throwable（所有异常的根）                   │
├────────────────────────────┬────────────────────────────────────┤
│                            │                                    │
│         Error              │           Exception                │
│    （严重错误，不处理）     │        （可处理异常）               │
│                            │                                    │
│   ┌──────────────────┐     │    ┌──────────────────────────┐    │
│   │ OutOfMemoryError │     │    │   Checked Exception      │    │
│   │   内存溢出        │     │    │  （编译期检查，必须处理）  │    │
│   ├──────────────────┤     │    ├──────────────────────────┤    │
│   │StackOverflowError│     │    │    IOException           │    │
│   │   栈溢出          │     │    │    SQLException          │    │
│   │   ...            │     │    │    ClassNotFoundException│    │
│   └──────────────────┘     │    └──────────────────────────┘    │
│                            │                                    │
│                            │    ┌──────────────────────────┐    │
│                            │    │  Unchecked Exception     │    │
│                            │    │  （运行时，不强制处理）    │    │
│                            │    ├──────────────────────────┤    │
│                            │    │  NullPointerException    │    │
│                            │    │  ArrayIndexOutOfBounds   │    │
│                            │    │  IllegalArgumentException│    │
│                            │    └──────────────────────────┘    │
│                            │                                    │
└────────────────────────────┴────────────────────────────────────┘
\`\`\`

### Checked vs Unchecked 对比表

| 特性 | Checked Exception | Unchecked Exception |
| :--- | :--- | :--- |
| **检查时机** | 编译期 | 运行期 |
| **是否强制处理** | ✅ 必须 | ❌ 不强制 |
| **处理方式** | try-catch / throws | 建议处理但不强制 |
| **典型代表** | IOException, SQLException | NullPointerException, RuntimeException |
| **出现原因** | 外部因素（环境/资源） | 代码逻辑错误（Bug） |

### 代码示例

\`\`\`java
// Checked：必须处理，否则编译报错
public void readFile() {
    // ❌ 不处理会编译错误
    FileInputStream fis = new FileInputStream("file.txt");
    
    // ✅ 正确处理
    try {
        FileInputStream fis = new FileInputStream("file.txt");
    } catch (IOException e) {
        e.printStackTrace();
    }
}

// Unchecked：不强制处理，但建议处理
public void process() {
    String s = null;
    // 可能抛出 NullPointerException，但不强制处理
    s.length();  // 运行时才会报错
}
\`\`\``
  },
  {
    question: '抽象类和接口的区别？Java 8 之后接口有什么变化？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `## 通俗理解

想象设计一辆车：
- **抽象类**：车的骨架，有轮子、引擎这些共同部件（is-a关系）
- **接口**：车的功能，能跑、能鸣笛（has-a能力）
- 一个车只能继承一个骨架（单继承），但可以有多种能力（多实现）

### 对比表

| 特性 | 抽象类 (Abstract Class) | 接口 (Interface) |
| :--- | :--- | :--- |
| **继承/实现** | 单继承 | 多实现 |
| **构造方法** | ✅ 有 | ❌ 无 |
| **成员变量** | 任意类型 | 只能是 \`public static final\` |
| **方法实现** | 可以有具体实现 | Java 8 后支持 \`default\`/\`static\` |
| **设计目的** | 侧重身份 (is-a) | 侧重能力 (has-a) |

### Java 8+ 接口进化

\`\`\`mermaid
graph TD
    subgraph J7 ["Java 7 及以前"]
        A["只能定义抽象方法<br/>(public abstract)"]
    end
    
    subgraph J8 ["Java 8 / 9"]
        B["default 默认实现"]
        C["static 静态工具方法"]
        D["private 私有助手方法"]
    end
    
    J7 --> J8
\`\`\`

### 实际应用场景

\`\`\`java
// 抽象类：定义是什么（模板）
public abstract class Vehicle {
    protected String brand;  // 品牌
    
    public Vehicle(String brand) {
        this.brand = brand;
    }
    
    public abstract void move();  // 抽象：怎么移动由子类决定
    
    public void honk() {  // 具体：默认实现
        System.out.println("哔哔！");
    }
}

// 接口：定义能做什么（能力）
public interface Flyable {
    void fly();  // 能飞
}

public interface Swimmable {
    void swim(); // 能游
}

// 具体类：可以继承一个类，实现多个接口
public class AmphibiousPlane extends Vehicle implements Flyable, Swimmable {
    @Override
    public void move() { /* ... */ }
    
    @Override
    public void fly() { /* ... */ }
    
    @Override
    public void swim() { /* ... */ }
}
\`\`\``
  },
  {
    question: '重写（Override）和重载（Overload）的区别？',
    tags: [{ text: '基础', type: 'frequent' }],
    answer: `## 通俗理解

想象"打电话"这个动作：
- **重载（Overload）**：同一个手机，可以语音通话、视频通话、发消息（同一个类，不同参数）
- **重写（Override）**：你继承了爸爸的手机，但把铃声改成了自己喜欢的（子类改父类方法）

### 对比图

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                      重载 vs 重写                                │
├──────────────────┬──────────────────┬───────────────────────────┤
│      特性        │     重载          │         重写              │
├──────────────────┼──────────────────┼───────────────────────────┤
│   位置           │   同一个类        │      子类对父类            │
│   方法名         │   必须相同        │      必须相同              │
│   参数列表       │   必须不同        │      必须相同              │
│   返回类型       │   可以不同        │      必须相同或子类型       │
│   访问修饰符     │   可以不同        │      不能更严格            │
│   异常           │   可以不同        │      不能更宽泛            │
│   多态类型       │   编译时多态      │      运行时多态            │
│   绑定方式       │   静态绑定        │      动态绑定              │
└──────────────────┴──────────────────┴───────────────────────────┘
\`\`\`

### 重载示例

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                        方法重载                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   class Calculator {                                            │
│       // 重载1：两个整数                                         │
│       int add(int a, int b) {                                   │
│           return a + b;                                         │
│       }                                                         │
│                                                                 │
│       // 重载2：三个整数                                         │
│       int add(int a, int b, int c) {                            │
│           return a + b + c;                                     │
│       }                                                         │
│                                                                 │
│       // 重载3：两个小数                                         │
│       double add(double a, double b) {                          │
│           return a + b;                                         │
│       }                                                         │
│   }                                                             │
│                                                                 │
│   编译时根据参数决定调用哪个：                                    │
│   calc.add(1, 2);        → 调用重载1                            │
│   calc.add(1, 2, 3);     → 调用重载2                            │
│   calc.add(1.5, 2.5);    → 调用重载3                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 重写示例

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                        方法重写                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   class Animal {                                                │
│       void makeSound() {                                        │
│           System.out.println("动物叫");                          │
│       }                                                         │
│   }                                                             │
│                                                                 │
│   class Dog extends Animal {                                    │
│       @Override                                                 │
│       void makeSound() {  // 重写父类方法                        │
│           System.out.println("汪汪！");                          │
│       }                                                         │
│   }                                                             │
│                                                                 │
│   class Cat extends Animal {                                    │
│       @Override                                                 │
│       void makeSound() {  // 重写父类方法                        │
│           System.out.println("喵喵！");                          │
│       }                                                         │
│   }                                                             │
│                                                                 │
│   // 运行时多态                                                  │
│   Animal a1 = new Dog();                                        │
│   Animal a2 = new Cat();                                        │
│   a1.makeSound();  // 输出：汪汪！（实际调用Dog的方法）          │
│   a2.makeSound();  // 输出：喵喵！（实际调用Cat的方法）          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 特别注意

\`\`\`java
// ❌ 静态方法不能被重写，只能被隐藏
class Parent {
    static void show() { System.out.println("Parent"); }
}

class Child extends Parent {
    static void show() { System.out.println("Child"); }  // 隐藏，不是重写
}

// ❌ private 方法不能被重写
class Parent {
    private void secret() {}  // 子类看不到
}
\`\`\``
  },
]

export const javaAdvancedQA: QAItem[] = [
  {
    question: 'Java 内存模型（JMM）是什么？volatile 关键字的作用？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `## 通俗理解

想象一个班级：
- **主内存**：教室的黑板（共享数据）
- **线程工作内存**：每个学生的笔记本（缓存）
- **JMM**：规定学生什么时候看黑板、什么时候把笔记写回黑板

### JMM 内存模型图

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                     Java 内存模型（JMM）                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                      ┌─────────────┐                            │
│                      │   主内存     │                            │
│                      │  （共享变量） │                            │
│                      │   count=0   │                            │
│                      └──────┬──────┘                            │
│                             │                                   │
│              ┌──────────────┼──────────────┐                    │
│              │              │              │                    │
│              ▼              ▼              ▼                    │
│      ┌─────────────┐ ┌─────────────┐ ┌─────────────┐           │
│      │  线程A工作   │ │  线程B工作   │ │  线程C工作   │           │
│      │   内存      │ │   内存      │ │   内存      │           │
│      │  count=0   │ │  count=0   │ │  count=0   │           │
│      └─────────────┘ └─────────────┘ └─────────────┘           │
│                                                                 │
│   每个线程有自己的工作内存，操作的是副本，需要同步回主内存         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### JMM 三大特性

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    JMM 三大特性                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 原子性（Atomicity）                                         │
│     ┌──────────────────────────────────────┐                    │
│     │ 操作要么全做，要么全不做，不可分割    │                    │
│     │ i = 1;          → 原子               │                    │
│     │ i++;            → 非原子（读-改-写）  │                    │
│     └──────────────────────────────────────┘                    │
│                                                                 │
│  2. 可见性（Visibility）                                        │
│     ┌──────────────────────────────────────┐                    │
│     │ 一个线程修改，其他线程立即可见        │                    │
│     │ 线程A修改 → 刷回主内存 → 线程B读取    │                    │
│     └──────────────────────────────────────┘                    │
│                                                                 │
│  3. 有序性（Ordering）                                          │
│     ┌──────────────────────────────────────┐                    │
│     │ 禁止指令重排序，保证执行顺序          │                    │
│     │ 编译器/CPU可能重排指令，JMM规定顺序   │                    │
│     └──────────────────────────────────────┘                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### volatile 的作用

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                     volatile 关键字                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  作用1：保证可见性                                               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   线程A修改volatile变量 ──→ 立即刷回主内存 ──→ 线程B可见   │   │
│  │                                                          │   │
│  │   普通变量：线程A修改 → 可能一直留在工作内存 → 线程B看不到 │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  作用2：禁止指令重排序                                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   插入内存屏障，确保代码执行顺序                           │   │
│  │                                                          │   │
│  │   volatile写之前的代码 → 屏障 → volatile写                │   │
│  │   volatile读 → 屏障 → volatile读之后的代码                │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ❌ 不能保证原子性                                               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │   volatile int count = 0;                                │   │
│  │   count++;  // 非原子操作！                               │   │
│  │   // 实际：读取(0) → 加1 → 写回(1)                        │   │
│  │   // 多线程可能都读到0，都写回1，丢失一次                  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### volatile 使用场景

\`\`\`java
// ✅ 状态标志
volatile boolean running = true;

public void stop() {
    running = false;  // 所有线程立即可见
}

// ✅ 单例模式（双重检查锁定）
class Singleton {
    private volatile static Singleton instance;
    
    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) {
                    instance = new Singleton();  // 禁止重排序
                }
            }
        }
        return instance;
    }
}

// ❌ 不适合计数器
volatile int count = 0;
count++;  // 线程不安全，需要用 AtomicInteger
\`\`\``
  },
  {
    question: 'synchronized 的底层原理？锁升级过程？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `## 通俗理解

想象公共厕所：
- **无锁**：厕所没人，直接进
- **偏向锁**：一直是同一个人用，门牌写他名字，不用竞争
- **轻量级锁**：偶尔有人排队，大家在门口自旋等待（不停看门）
- **重量级锁**：排队人太多，大家去休息室睡觉，轮到了再叫醒

### synchronized 原理图

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                   synchronized 底层原理                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  每个对象头中有一个 Mark Word，存储锁信息                         │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                     对象头结构                            │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  Mark Word (64位)                                        │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  锁状态标记 │ 偏向线程ID │ 分代年龄 │ 指向锁记录指针 │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                          │   │
│  │  Klass Pointer（指向类元数据）                            │   │
│  │  Array Length（数组对象才有）                             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  字节码层面：                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  monitorenter  // 进入同步块，获取锁                       │   │
│  │     ...                                                   │   │
│  │  monitorexit  // 退出同步块，释放锁                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 锁升级过程

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                     锁升级过程（JDK 1.6+）                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   无锁 ──→ 偏向锁 ──→ 轻量级锁 ──→ 重量级锁                      │
│   (01)      (01)        (00)          (10)                      │
│                                                                 │
│   ┌─────────┐                                                   │
│   │  无锁    │  ← 对象刚创建，没有线程访问                        │
│   │  状态    │                                                   │
│   └────┬────┘                                                   │
│        │ 第一个线程进入                                          │
│        ▼                                                        │
│   ┌─────────┐                                                   │
│   │ 偏向锁   │  ← 只有一个线程使用，记录线程ID                    │
│   │  状态    │     下次同线程进入，无需CAS，直接访问              │
│   └────┬────┘                                                   │
│        │ 第二个线程尝试进入                                       │
│        ▼                                                        │
│   ┌─────────┐                                                   │
│   │ 轻量级锁 │  ← 有竞争但交替执行，线程自旋等待                  │
│   │  状态    │     使用CAS尝试获取锁                              │
│   └────┬────┘                                                   │
│        │ 自旋超过10次或CPU核数一半                                │
│        ▼                                                        │
│   ┌─────────┐                                                   │
│   │ 重量级锁 │  ← 竞争激烈，线程阻塞，等待唤醒                    │
│   │  状态    │     使用操作系统Mutex，性能低但省CPU               │
│   └─────────┘                                                   │
│                                                                 │
│   注意：锁只能升级，不能降级（偏向锁可以撤销）                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 锁优化技术

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                     锁优化技术                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 锁消除（Lock Elimination）                                  │
│     ┌──────────────────────────────────────────────────────┐    │
│     │                                                      │    │
│     │   StringBuffer sb = new StringBuffer();              │    │
│     │   sb.append("a");  // sb不会逃逸出方法               │    │
│     │                                                      │    │
│     │   编译器发现sb只在方法内使用，消除synchronized        │    │
│     │                                                      │    │
│     └──────────────────────────────────────────────────────┘    │
│                                                                 │
│  2. 锁粗化（Lock Coarsening）                                   │
│     ┌──────────────────────────────────────────────────────┐    │
│     │                                                      │    │
│     │   for (int i = 0; i < 100; i++) {                    │    │
│     │       synchronized(obj) { ... }  // 频繁加锁解锁     │    │
│     │   }                                                  │    │
│     │                                                      │    │
│     │   优化后：把锁放到循环外，减少锁操作次数               │    │
│     │   synchronized(obj) {                                │    │
│     │       for (...) { ... }                              │    │
│     │   }                                                  │    │
│     │                                                      │    │
│     └──────────────────────────────────────────────────────┘    │
│                                                                 │
│  3. 自适应自旋（Adaptive Spinning）                             │
│     ┌──────────────────────────────────────────────────────┐    │
│     │                                                      │    │
│     │   根据历史成功率动态调整自旋次数                       │    │
│     │   - 上次成功了 → 多自旋几次                           │    │
│     │   - 上次失败了 → 少自旋，早点阻塞                     │    │
│     │                                                      │    │
│     └──────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\``
  },
  {
    question: 'ThreadLocal 的原理和使用场景？内存泄漏问题？',
    tags: [{ text: '高频', type: 'frequent' }],
    answer: `## 通俗理解

想象每个人都有自己的储物柜：
- **ThreadLocal**：给每个线程一个独立的"储物柜"
- 线程A放的东西，线程B看不到，互不干扰
- 就像每个人的手机相册，各自存各自的照片

### ThreadLocal 原理图

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    ThreadLocal 原理                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   每个 Thread 对象内部有一个 ThreadLocalMap                      │
│                                                                 │
│   ┌──────────────────────────────────────────────────────────┐  │
│   │                      Thread 对象                          │  │
│   │  ┌────────────────────────────────────────────────────┐  │  │
│   │  │  ThreadLocalMap                                    │  │  │
│   │  │  ┌──────────────────────────────────────────────┐  │  │  │
│   │  │  │ Entry[] table                                  │  │  │  │
│   │  │  │  ┌─────────────┬─────────────┬─────────────┐  │  │  │  │
│   │  │  │  │ Entry[0]    │ Entry[1]    │ Entry[2]    │  │  │  │  │
│   │  │  │  │ ┌───┬───┐   │ ┌───┬───┐   │ ┌───┬───┐   │  │  │  │  │
│   │  │  │  │ │Key│Val│   │ │Key│Val│   │ │Key│Val│   │  │  │  │  │
│   │  │  │  │ │弱 │   │   │ │弱 │   │   │ │弱 │   │   │  │  │  │  │
│   │  │  │  │ │引 │   │   │ │引 │   │   │ │引 │   │   │  │  │  │  │
│   │  │  │  │ │用 │   │   │ │用 │   │   │ │用 │   │   │  │  │  │  │
│   │  │  │  │ └───┴───┘   │ └───┴───┘   │ └───┴───┘   │  │  │  │  │
│   │  │  │  └─────────────┴─────────────┴─────────────┘  │  │  │  │
│   │  │  └──────────────────────────────────────────────┘  │  │  │
│   │  └────────────────────────────────────────────────────┘  │  │
│   └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│   Key = ThreadLocal 对象（弱引用）                               │
│   Value = 存储的数据（强引用）                                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 使用场景

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    ThreadLocal 使用场景                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 用户登录信息传递                                             │
│     ┌──────────────────────────────────────────────────────┐    │
│     │                                                      │    │
│     │   拦截器 ──→ ThreadLocal存储用户信息 ──→ 各层使用    │    │
│     │                                                      │    │
│     │   Controller → Service → DAO 都能获取当前用户        │    │
│     │   不用每个方法都传User参数                             │    │
│     │                                                      │    │
│     └──────────────────────────────────────────────────────┘    │
│                                                                 │
│  2. 数据库连接管理                                               │
│     ┌──────────────────────────────────────────────────────┐    │
│     │                                                      │    │
│     │   每个线程一个Connection，保证事务一致性               │    │
│     │                                                      │    │
│     │   Service层开启事务                                    │    │
│     │     └── DAO层使用同一个Connection                      │    │
│     │         └── 提交/回滚                                  │    │
│     │                                                      │    │
│     └──────────────────────────────────────────────────────┘    │
│                                                                 │
│  3. SimpleDateFormat 线程安全                                   │
│     ┌──────────────────────────────────────────────────────┐    │
│     │                                                      │    │
│     │   // 每个线程一个SimpleDateFormat                     │    │
│     │   private static ThreadLocal<SimpleDateFormat>       │    │
│     │       tl = ThreadLocal.withInitial(() ->             │    │
│     │           new SimpleDateFormat("yyyy-MM-dd"));       │    │
│     │                                                      │    │
│     └──────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 内存泄漏问题

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                  ThreadLocal 内存泄漏                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  问题原因：                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   ThreadLocalMap 的 Entry：                              │   │
│  │   Key（ThreadLocal）是 弱引用 ──→ GC可回收               │   │
│  │   Value（数据）是 强引用 ───────→ GC不可回收             │   │
│  │                                                          │   │
│  │   线程池场景：                                             │   │
│  │   - 线程不销毁，ThreadLocalMap一直存在                    │   │
│  │   - ThreadLocal被GC回收，Key变成null                      │   │
│  │   - 但Value还在，无法访问也无法回收 → 内存泄漏            │   │
│  │                                                          │   │
│  │   图示：                                                   │   │
│  │   ┌─────────────────┐                                    │   │
│  │   │  ThreadLocalMap │                                    │   │
│  │   │  ┌───────────┐  │                                    │   │
│  │   │  │ Key=null  │  │  ← ThreadLocal被GC回收             │   │
│  │   │  │ Value=大对象│  │  ← 但Value还在，泄漏！            │   │
│  │   │  └───────────┘  │                                    │   │
│  │   └─────────────────┘                                    │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  解决方案：                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   try {                                                  │   │
│  │       threadLocal.set(value);                            │   │
│  │       // 业务逻辑                                         │   │
│  │   } finally {                                            │   │
│  │       threadLocal.remove();  // 必须手动移除！            │   │
│  │   }                                                      │   │
│  │                                                          │   │
│  │   或使用 try-with-resources 模式                         │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\``
  },
  {
    question: 'Java 中的四种引用类型？使用场景？',
    tags: [{ text: '重要', type: 'important' }],
    answer: `## 通俗理解

想象租房：
- **强引用**：签了长期合同，房东不能赶你走（不回收）
- **软引用**：签了短期合同，房东说人多了可能要你搬（内存不足回收）
- **弱引用**：没签合同，只是口头说想租，随时可能被赶走（下次GC回收）
- **虚引用**：只是登记了一下，根本住不进去，搬走了才知道（跟踪回收）

### 四种引用对比

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    四种引用类型对比                              │
├──────────────┬──────────────────┬───────────────────────────────┤
│    引用类型   │     回收时机      │           使用场景            │
├──────────────┼──────────────────┼───────────────────────────────┤
│   强引用      │   永远不回收      │   普通对象使用                │
│   软引用      │   内存不足回收    │   缓存（图片缓存等）          │
│   弱引用      │   下次GC回收      │   ThreadLocal、WeakHashMap    │
│   虚引用      │   随时可能回收    │   跟踪对象回收、管理堆外内存    │
└──────────────┴──────────────────┴───────────────────────────────┘
\`\`\`

### 引用关系图

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    引用强度关系                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   强引用 ───────────────────────────────────────────────→       │
│     │                                                           │
│     │  Object obj = new Object();  // 强引用                    │
│     │  obj = null;  // 断开引用，才能被回收                      │
│     │                                                           │
│     ▼                                                           │
│   软引用 ───────────────────────────────→ 内存不足时回收         │
│     │                                                           │
│     │  SoftReference<byte[]> softRef =                          │
│     │      new SoftReference<>(new byte[1024*1024*10]);         │
│     │  byte[] data = softRef.get();  // 可能返回null            │
│     │                                                           │
│     ▼                                                           │
│   弱引用 ───────────────────→ 下次GC时回收                       │
│     │                                                           │
│     │  WeakReference<Object> weakRef =                          │
│     │      new WeakReference<>(obj);                            │
│     │  // GC后 weakRef.get() 返回null                          │
│     │                                                           │
│     ▼                                                           │
│   虚引用 ──→ 随时可能回收，无法通过引用获取对象                   │
│                                                                 │
│   引用强度：强 > 软 > 弱 > 虚                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 使用场景详解

\`\`\`java
// 1. 强引用 - 默认
Object strongRef = new Object();

// 2. 软引用 - 缓存场景
// 适合：内存敏感的缓存，如图片缓存
SoftReference<byte[]> softCache = 
    new SoftReference<>(new byte[1024 * 1024 * 100]); // 100MB

// 使用缓存
byte[] cacheData = softCache.get();
if (cacheData != null) {
    // 缓存还在，直接使用
} else {
    // 缓存被回收，重新加载
    cacheData = loadFromDisk();
    softCache = new SoftReference<>(cacheData);
}

// 3. 弱引用 - ThreadLocal、WeakHashMap
// ThreadLocalMap 的 Entry 使用弱引用
// 防止 ThreadLocal 不被回收导致的内存泄漏

// 4. 虚引用 - 跟踪对象回收、管理堆外内存
// DirectByteBuffer 使用虚引用跟踪堆外内存回收
ReferenceQueue<Object> queue = new ReferenceQueue<>();
PhantomReference<Object> phantomRef = 
    new PhantomReference<>(obj, queue);

// 无法通过 phantomRef.get() 获取对象，总是返回 null
// 但当 obj 被回收时，phantomRef 会被加入 queue
// 可以据此做清理工作（如释放堆外内存）
\`\`\``
  },
  {
    question: 'JVM 垃圾回收算法有哪些？G1 和 CMS 的区别？',
    tags: [{ text: '必问', type: 'must' }],
    answer: `## 通俗理解

想象整理房间：
- **标记-清除**：把不要的东西标记出来扔掉，但地上会留下坑（内存碎片）
- **复制**：把要的东西搬到新房间，旧房间直接清空（无碎片但浪费空间）
- **标记-整理**：把要的东西标记出来，然后全部推到一边（无碎片但慢）

### 垃圾回收算法

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    垃圾回收算法                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 标记-清除（Mark-Sweep）                                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   阶段1：标记存活对象                                       │   │
│  │   ┌───┬───┬───┬───┬───┬───┐                              │   │
│  │   │ ✓ │ ✗ │ ✓ │ ✗ │ ✓ │ ✗ │  ✓=存活  ✗=垃圾              │   │
│  │   └───┴───┴───┴───┴───┴───┘                              │   │
│  │                                                          │   │
│  │   阶段2：清除垃圾                                          │   │
│  │   ┌───┬───┬───┬───┬───┬───┐                              │   │
│  │   │ ✓ │   │ ✓ │   │ ✓ │   │  ← 产生碎片！                │   │
│  │   └───┴───┴───┴───┴───┴───┘                              │   │
│  │                                                          │   │
│  │   优点：简单，不需要移动对象                                │   │
│  │   缺点：产生内存碎片，分配大对象困难                        │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  2. 复制（Copying）                                             │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   内存分两块：From 和 To                                   │   │
│  │                                                          │   │
│  │   From空间：┌───┬───┬───┬───┐  To空间：┌───┬───┬───┬───┐  │   │
│  │             │ ✓ │ ✗ │ ✓ │ ✗ │           │   │   │   │   │  │   │
│  │             └───┴───┴───┴───┘           └───┴───┴───┴───┘  │   │
│  │                                                          │   │
│  │   复制存活对象到To空间：                                    │   │
│  │   From空间：┌───┬───┬───┬───┐  To空间：┌───┬───┬───┬───┐  │   │
│  │             │   │   │   │   │           │ ✓ │ ✓ │   │   │  │   │
│  │             └───┴───┴───┴───┘           └───┴───┴───┴───┘  │   │
│  │                                                          │   │
│  │   清空From，交换角色                                        │   │
│  │                                                          │   │
│  │   优点：无碎片，分配简单                                    │   │
│  │   缺点：内存利用率只有50%                                   │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  3. 标记-整理（Mark-Compact）                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   阶段1：标记存活对象                                       │   │
│  │   ┌───┬───┬───┬───┬───┬───┐                              │   │
│  │   │ ✓ │ ✗ │ ✓ │ ✗ │ ✓ │ ✗ │                              │   │
│  │   └───┴───┴───┴───┴───┴───┘                              │   │
│  │                                                          │   │
│  │   阶段2：整理，存活对象向一端移动                           │   │
│  │   ┌───┬───┬───┬───┬───┬───┐                              │   │
│  │   │ ✓ │ ✓ │ ✓ │   │   │   │  ← 无碎片，连续空间          │   │
│  │   └───┴───┴───┴───┴───┴───┘                              │   │
│  │                                                          │   │
│  │   优点：无碎片                                             │   │
│  │   缺点：需要移动对象，STW时间长                             │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### G1 vs CMS 对比

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                      G1 vs CMS 对比                              │
├──────────────────┬──────────────────┬───────────────────────────┤
│      特性        │       CMS        │           G1              │
├──────────────────┼──────────────────┼───────────────────────────┤
│   设计目标       │   低延迟          │   平衡吞吐量和延迟         │
│   内存划分       │  老年代/新生代     │   Region（1-32MB）        │
│   碎片问题       │   有碎片          │   整理，无碎片             │
│   可预测停顿     │   否             │   是（-XX:MaxGCPauseMillis）│
│   JDK版本        │   1.5            │   1.7（9+默认）            │
│   适用场景       │   小堆内存        │   大堆内存（>4GB）         │
└──────────────────┴──────────────────┴───────────────────────────┘
\`\`\`

### G1 垃圾收集器详解

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                      G1 收集器                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  内存划分：                                                      │
│  ┌────┬────┬────┬────┬────┬────┬────┬────┬────┬────┐           │
│  │ E  │ S  │ S  │ O  │ O  │ O  │ O  │ H  │ O  │ O  │           │
│  └────┴────┴────┴────┴────┴────┴────┴────┴────┴────┘           │
│   E=Eden  S=Survivor  O=Old  H=Humongous（大对象）              │
│                                                                 │
│  特点：                                                          │
│  1. 将堆划分为多个 Region（默认2048个）                         │
│  2. 每个 Region 角色不固定，可以是Eden/Survivor/Old             │
│  3. 优先回收垃圾最多的 Region（Garbage First）                  │
│  4. 使用标记-整理算法，无内存碎片                                │
│  5. 可设置目标停顿时间（默认200ms）                             │
│                                                                 │
│  回收过程：                                                      │
│  Young GC：回收 Eden + Survivor → 并行复制到 Survivor/Old      │
│  Mixed GC：回收 Young + 部分 Old（根据优先级）                  │
│  Full GC：  单线程回收整个堆（尽量避免）                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\``
  },
]

export const javaSpringQA: QAItem[] = [
  {
    question: 'Spring 的 IOC 和 AOP 是什么？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `## 通俗理解

想象一个餐厅：
- **IOC（控制反转）**：以前厨师自己买菜（自己创建依赖），现在由采购部统一配送（Spring容器管理），厨师只负责做菜
- **AOP（面向切面）**：每个厨师做菜前后，服务员自动记录时间、打扫卫生（横切关注点），厨师不用管这些

### IOC 控制反转

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    IOC 控制反转                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  传统方式（自己控制）：                                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   class UserService {                                    │   │
│  │       private UserDao userDao = new UserDao();  // 自己创建│   │
│  │   }                                                      │   │
│  │                                                          │   │
│  │   问题：耦合度高，UserDao实现变了，UserService也要改      │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  IOC方式（Spring控制）：                                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   ┌──────────────┐      注入       ┌──────────────┐      │   │
│  │   │ Spring容器    │ ─────────────→ │ UserService  │      │   │
│  │   │              │                │ 需要UserDao   │      │   │
│  │   │ 管理所有Bean  │ ←───────────── │              │      │   │
│  │   │  UserDao     │    使用         └──────────────┘      │   │
│  │   └──────────────┘                                      │   │
│  │                                                          │   │
│  │   好处：解耦，UserService不需要知道UserDao怎么创建        │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  依赖注入（DI）的三种方式：                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   1. 构造器注入（推荐）                                    │   │
│  │      @Autowired                                          │   │
│  │      public UserService(UserDao userDao) {               │   │
│  │          this.userDao = userDao;                         │   │
│  │      }                                                   │   │
│  │                                                          │   │
│  │   2. Setter注入                                          │   │
│  │      @Autowired                                          │   │
│  │      public void setUserDao(UserDao userDao) {           │   │
│  │          this.userDao = userDao;                         │   │
│  │      }                                                   │   │
│  │                                                          │   │
│  │   3. 字段注入（不推荐）                                    │   │
│  │      @Autowired                                          │   │
│  │      private UserDao userDao;                            │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### AOP 面向切面编程

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    AOP 面向切面编程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  核心概念：                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   Aspect（切面）：横切关注点的模块化                        │   │
│  │   ┌──────────────────────────────────────────┐            │   │
│  │   │  日志记录 / 事务管理 / 权限检查 / 性能监控 │            │   │
│  │   └──────────────────────────────────────────┘            │   │
│  │                                                          │   │
│  │   Join Point（连接点）：程序执行点（方法调用、异常抛出等）   │   │
│  │                                                          │   │
│  │   Pointcut（切点）：匹配连接点的表达式                       │   │
│  │   execution(* com.service.*.*(..))  // 匹配service包下所有方法│   │
│  │                                                          │   │
│  │   Advice（通知）：切面的具体动作                             │   │
│  │   @Before / @After / @AfterReturning / @AfterThrowing     │   │
│  │                                                          │   │
│  │   Weaving（织入）：将切面应用到目标对象                      │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  执行流程：                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   @Before 前置通知                                         │   │
│  │        ↓                                                 │   │
│  │   目标方法执行                                              │   │
│  │        ↓                                                 │   │
│  │   @AfterReturning 返回通知 / @AfterThrowing 异常通知       │   │
│  │        ↓                                                 │   │
│  │   @After 后置通知                                          │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  实现方式：                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   1. JDK 动态代理：目标类实现了接口                        │   │
│  │      生成接口的代理类，实现接口方法                         │   │
│  │                                                          │   │
│  │   2. CGLIB 代理：目标类没有实现接口                        │   │
│  │      生成目标类的子类，重写父类方法                         │   │
│  │                                                          │   │
│  │   Spring Boot 2.x+ 默认使用 CGLIB                         │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\``
  },
  {
    question: 'Spring Bean 的生命周期？作用域有哪些？',
    tags: [{ text: '高频', type: 'frequent' }],
    answer: `## Bean 生命周期

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                  Spring Bean 生命周期                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 实例化（Instantiation）                                     │
│     ┌──────────────────────────────────────────────────────┐    │
│     │  调用构造方法创建对象                                   │    │
│     │  Bean bean = new Bean();                              │    │
│     └──────────────────────────────────────────────────────┘    │
│                          ↓                                      │
│  2. 属性赋值（Populate）                                        │
│     ┌──────────────────────────────────────────────────────┐    │
│     │  依赖注入，设置属性值                                   │    │
│     │  bean.setUserDao(userDao);                            │    │
│     └──────────────────────────────────────────────────────┘    │
│                          ↓                                      │
│  3. 初始化（Initialization）                                    │
│     ┌──────────────────────────────────────────────────────┐    │
│     │  ① 调用 Aware 接口方法（获取容器资源）                 │    │
│     │     setBeanName(), setApplicationContext()            │    │
│     │                                                      │    │
│     │  ② @PostConstruct 方法                               │    │
│     │                                                      │    │
│     │  ③ InitializingBean.afterPropertiesSet()             │    │
│     │                                                      │    │
│     │  ④ 自定义 init-method                                │    │
│     └──────────────────────────────────────────────────────┘    │
│                          ↓                                      │
│  4. 使用期（In Use）                                            │
│     ┌──────────────────────────────────────────────────────┐    │
│     │  Bean 可以被使用                                        │    │
│     └──────────────────────────────────────────────────────┘    │
│                          ↓                                      │
│  5. 销毁（Destruction）                                         │
│     ┌──────────────────────────────────────────────────────┐    │
│     │  ① @PreDestroy 方法                                   │    │
│     │                                                      │    │
│     │  ② DisposableBean.destroy()                          │    │
│     │                                                      │    │
│     │  ③ 自定义 destroy-method                             │    │
│     └──────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### Bean 作用域

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    Bean 作用域                                   │
├──────────────────┬──────────────────────────────────────────────┤
│      作用域       │                    说明                       │
├──────────────────┼──────────────────────────────────────────────┤
│   singleton      │  单例（默认），整个容器只有一个实例            │
│                  │  ┌──────────────────────────────────────┐    │
│                  │  │  Spring容器                           │    │
│                  │  │  ┌────────────────────────────────┐  │    │
│                  │  │  │      UserService Bean          │  │    │
│                  │  │  │   所有地方@Autowired都是这个    │  │    │
│                  │  │  └────────────────────────────────┘  │    │
│                  │  └──────────────────────────────────────┘    │
├──────────────────┼──────────────────────────────────────────────┤
│   prototype      │  每次获取都创建新实例                         │
│                  │  ┌──────────────────────────────────────┐    │
│                  │  │  getBean() → 新 Bean                 │    │
│                  │  │  getBean() → 新 Bean                 │    │
│                  │  │  @Autowired → 新 Bean                │    │
│                  │  └──────────────────────────────────────┘    │
├──────────────────┼──────────────────────────────────────────────┤
│   request        │  每个 HTTP 请求一个实例                       │
│   session        │  每个 HTTP Session 一个实例                  │
│   application    │  每个 ServletContext 一个实例                │
└──────────────────┴──────────────────────────────────────────────┘
\`\`\`

### 代码示例

\`\`\`java
@Component
public class MyBean implements InitializingBean, DisposableBean {
    
    @PostConstruct
    public void postConstruct() {
        System.out.println("1. @PostConstruct 执行");
    }
    
    @Override
    public void afterPropertiesSet() {
        System.out.println("2. InitializingBean 执行");
    }
    
    public void init() {
        System.out.println("3. 自定义 init-method 执行");
    }
    
    @PreDestroy
    public void preDestroy() {
        System.out.println("4. @PreDestroy 执行");
    }
    
    @Override
    public void destroy() {
        System.out.println("5. DisposableBean 执行");
    }
}
\`\`\``
  },
  {
    question: 'Spring Boot 的自动配置原理？Starter 机制？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `## 自动配置原理

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                Spring Boot 自动配置原理                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  入口：@SpringBootApplication                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  @SpringBootApplication                                  │   │
│  │  ├── @Configuration          // 标记为配置类             │   │
│  │  ├── @EnableAutoConfiguration // 开启自动配置 ★         │   │
│  │  └── @ComponentScan          // 组件扫描                 │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  @EnableAutoConfiguration 原理：                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  1. 读取 META-INF/spring.factories                        │   │
│  │                                                          │   │
│  │     ┌────────────────────────────────────────────────┐   │   │
│  │     │ # Auto Configure                               │   │   │
│  │     │ org.springframework.boot.autoconfigure.EnableAutoConfiguration=│
│  │     │ org.springframework.boot.autoconfigure.web.WebMvcAutoConfiguration,│
│  │     │ org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,│
│  │     │ ...                                            │   │   │
│  │     └────────────────────────────────────────────────┘   │   │
│  │                                                          │   │
│  │  2. 加载所有 AutoConfiguration 类                         │   │
│  │                                                          │   │
│  │  3. 根据条件注解判断是否生效                               │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  条件注解：                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  @ConditionalOnClass(DataSource.class)                   │   │
│  │  → 类路径有 DataSource 才生效                             │   │
│  │                                                          │   │
│  │  @ConditionalOnMissingBean(DataSource.class)             │   │
│  │  → 容器中没有 DataSource Bean 才生效                      │   │
│  │                                                          │   │
│  │  @ConditionalOnProperty(prefix="spring.datasource")      │   │
│  │  → 配置文件中配置了才生效                                  │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### Starter 机制

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    Starter 机制                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  什么是 Starter？                                                │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  预定义的依赖组合 + 自动配置                                │   │
│  │                                                          │   │
│  │  传统方式：                                               │   │
│  │  需要添加 spring-webmvc、jackson、tomcat 等一堆依赖        │   │
│  │                                                          │   │
│  │  Starter 方式：                                           │   │
│  │  只需添加 spring-boot-starter-web                        │   │
│  │  自动包含所有相关依赖 + 自动配置                            │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  常用 Starter：                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  spring-boot-starter-web          Web应用               │   │
│  │  spring-boot-starter-data-jpa     JPA访问数据库          │   │
│  │  spring-boot-starter-redis        Redis操作              │   │
│  │  spring-boot-starter-test         测试                  │   │
│  │  spring-boot-starter-security     安全                  │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  自定义 Starter：                                                │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  1. 创建自动配置类                                        │   │
│  │     @Configuration                                       │   │
│  │     @ConditionalOnClass(MyService.class)                 │   │
│  │     public class MyAutoConfiguration {                   │   │
│  │         @Bean                                            │   │
│  │         public MyService myService() {                   │   │
│  │             return new MyService();                      │   │
│  │         }                                                │   │
│  │     }                                                    │   │
│  │                                                          │   │
│  │  2. 创建 META-INF/spring.factories                        │   │
│  │     org.springframework.boot.autoconfigure.EnableAutoConfiguration=\\│   │
│  │     com.example.MyAutoConfiguration                      │   │
│  │                                                          │   │
│  │  3. 打包发布，其他项目引入即可自动配置                      │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\``
  },
  {
    question: 'Spring 事务的传播行为和隔离级别？',
    tags: [{ text: '高频', type: 'frequent' }],
    answer: `## 事务传播行为

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                  事务传播行为（Propagation）                      │
├──────────────────┬──────────────────────────────────────────────┤
│      行为        │                    说明                       │
├──────────────────┼──────────────────────────────────────────────┤
│   REQUIRED       │  默认。有事务就加入，没有就新建               │
│                  │  ┌──────────────────────────────────────┐    │
│                  │  │  方法A（有事务）                       │    │
│                  │  │    └── 方法B（加入A的事务）            │    │
│                  │  │                                        │    │
│                  │  │  方法A（无事务）                       │    │
│                  │  │    └── 方法B（新建事务）               │    │
│                  │  └──────────────────────────────────────┘    │
├──────────────────┼──────────────────────────────────────────────┤
│   REQUIRES_NEW   │  挂起当前事务，创建新事务                     │
│                  │  ┌──────────────────────────────────────┐    │
│                  │  │  方法A（事务A）                        │    │
│                  │  │    ├── 挂起A                           │    │
│                  │  │    ├── 方法B（事务B，独立提交/回滚）    │    │
│                  │  │    └── 恢复A                           │    │
│                  │  └──────────────────────────────────────┘    │
├──────────────────┼──────────────────────────────────────────────┤
│   NESTED         │  在当前事务中创建嵌套事务（保存点）           │
│                  │  B回滚不影响A，A回滚B也回滚                  │
├──────────────────┼──────────────────────────────────────────────┤
│   SUPPORTS       │  有事务就加入，没有就以非事务执行             │
├──────────────────┼──────────────────────────────────────────────┤
│   NOT_SUPPORTED  │  挂起当前事务，以非事务执行                   │
├──────────────────┼──────────────────────────────────────────────┤
│   NEVER          │  有事务就抛异常                               │
├──────────────────┼──────────────────────────────────────────────┤
│   MANDATORY      │  必须有事务，否则抛异常                       │
└──────────────────┴──────────────────────────────────────────────┘
\`\`\`

## 事务隔离级别

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                  事务隔离级别（Isolation）                        │
├──────────────────┬──────────────────────────────────────────────┤
│      级别        │    问题      │         说明                  │
├──────────────────┼──────────────┼───────────────────────────────┤
│   READ_UNCOMMITTED│ 脏读、不可重复读、幻读 │ 性能最好，安全性最差 │
│   READ_COMMITTED  │ 不可重复读、幻读       │ Oracle默认          │
│   REPEATABLE_READ │ 幻读                  │ MySQL默认           │
│   SERIALIZABLE    │ 无                   │ 性能最差，安全性最好 │
└──────────────────┴──────────────┴───────────────────────────────┘
\`\`\`

### 事务问题图解

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    事务并发问题                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 脏读（Dirty Read）                                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  事务A              事务B                                 │   │
│  │    │                  │                                  │   │
│  │    │  修改余额=100    │                                  │   │
│  │    │ ───────────────→ │                                  │   │
│  │    │  （未提交）       │  读取余额=100  ← 脏读！           │   │
│  │    │  回滚            │                                  │   │
│  │    │ ───────────────→ │  实际余额还是200，但读到了100     │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  2. 不可重复读（Non-repeatable Read）                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  事务A              事务B                                 │   │
│  │    │                  │                                  │   │
│  │    │  读取余额=100    │                                  │   │
│  │    │ ───────────────→ │  修改余额=200                    │   │
│  │    │  再次读取=200    │  （提交）                         │   │
│  │    │ ←─────────────── │                                  │   │
│  │    │  同一事务内两次读取结果不同！                         │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  3. 幻读（Phantom Read）                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  事务A              事务B                                 │   │
│  │    │                  │                                  │   │
│  │    │  查询：10条记录  │  插入1条记录                      │   │
│  │    │ ───────────────→ │  （提交）                         │   │
│  │    │  再次查询：11条  │                                  │   │
│  │    │ ←─────────────── │  像出现了幻觉！                   │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 事务失效场景

\`\`\`java
@Service
public class UserService {
    
    @Transactional
    public void methodA() {
        // 事务生效
        methodB();  // 同类调用，事务失效！
    }
    
    @Transactional
    public void methodB() {
        // 事务不生效（AOP代理问题）
    }
}

// 解决方案1：注入自身
@Autowired
private UserService self;

public void methodA() {
    self.methodB();  // 通过代理调用，事务生效
}

// 解决方案2：拆分到另一个类
// 解决方案3：使用 AopContext.currentProxy()
\`\`\``
  },
]

export const javaConcurrentQA: QAItem[] = [
  {
    question: 'ConcurrentHashMap 的原理？JDK 1.7 和 1.8 的区别？',
    tags: [{ text: '必问', type: 'must' }, { text: '高频', type: 'frequent' }],
    answer: `## 通俗理解

想象一个大型图书馆：
- **JDK 1.7**：把图书馆分成16个区域，每个区域一把锁，同一区域只能一个人
- **JDK 1.8**：不再分区，只在书架（桶）级别加锁，大部分操作不用锁

### JDK 1.7：Segment 分段锁

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│               JDK 1.7 ConcurrentHashMap                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  结构：Segment[] + HashEntry[][]                                │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Segment[16]                            │   │
│  │  ┌─────────┬─────────┬─────────┬─────────┬─────────┐     │   │
│  │  │Segment0 │Segment1 │Segment2 │  ...    │Segment15│     │   │
│  │  │  🔒     │  🔒     │  🔒     │         │  🔒     │     │   │
│  │  └────┬────┴────┬────┴────┬────┴─────────┴────┬────┘     │   │
│  │       │         │         │                   │          │   │
│  │       ▼         ▼         ▼                   ▼          │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐   ┌─────────┐      │   │
│  │  │HashEntry│ │HashEntry│ │HashEntry│   │HashEntry│      │   │
│  │  │ 链表/数组│ │ 链表/数组│ │ 链表/数组│   │ 链表/数组│      │   │
│  │  └─────────┘ └─────────┘ └─────────┘   └─────────┘      │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  特点：                                                          │
│  - 默认16个 Segment，最多16个线程并发                           │
│  - 每个 Segment 继承 ReentrantLock                              │
│  - put 时先定位 Segment，再加锁                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### JDK 1.8：CAS + synchronized

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│               JDK 1.8 ConcurrentHashMap                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  结构：Node[] + 链表/红黑树                                      │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                      Node[]                               │   │
│  │  ┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐       │   │
│  │  │  0  │  1  │  2  │  3  │  4  │  5  │ ... │  n  │       │   │
│  │  └─┬───┴──┬──┴──┬──┴──┬──┴──┬──┴──┬──┴─────┴──┬──┘       │   │
│  │    │      │     │     │     │     │           │          │   │
│  │    ▼      ▼     ▼     ▼     ▼     ▼           ▼          │   │
│  │  null   Node  Node  TreeNode Node  null      Node        │   │
│  │         ↓     ↓      ↓      ↓                ↓          │   │
│  │        Node  Node  TreeNode Node             Node        │   │
│  │              ↓              ↓                            │   │
│  │             Node           ...                           │   │
│  │                                                          │   │
│  │  链表长度 ≥ 8 且数组长度 ≥ 64 时转为红黑树                 │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  put 流程：                                                       │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  1. 计算 hash，确定数组位置                                │   │
│  │                                                          │   │
│  │  2. 位置为空：CAS 直接插入                                 │   │
│  │     ┌──────────────────────────────────────────────┐     │   │
│  │     │  compareAndSwapObject(node, null, newNode)   │     │   │
│  │     └──────────────────────────────────────────────┘     │   │
│  │                                                          │   │
│  │  3. 位置有值：synchronized 锁定首节点                      │   │
│  │     ┌──────────────────────────────────────────────┐     │   │
│  │     │  synchronized (firstNode) {                  │     │   │
│  │     │      // 遍历链表/红黑树，插入或更新             │     │   │
│  │     │  }                                           │     │   │
│  │     └──────────────────────────────────────────────┘     │   │
│  │                                                          │   │
│  │  4. 检查是否需要扩容或转红黑树                              │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  改进：                                                          │
│  - 取消 Segment，锁粒度更细（桶级别）                           │
│  - 使用 CAS + synchronized，性能更好                            │
│  - 引入红黑树，解决哈希冲突严重时的性能问题                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 对比总结

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    1.7 vs 1.8 对比                               │
├──────────────────┬──────────────────┬───────────────────────────┤
│      特性        │     JDK 1.7      │         JDK 1.8           │
├──────────────────┼──────────────────┼───────────────────────────┤
│   数据结构       │   Segment数组    │   Node数组                │
│   锁机制         │   Segment锁      │   CAS + synchronized      │
│   锁粒度         │   段级别         │   桶级别                  │
│   并发度         │   默认16         │   理论上无上限             │
│   哈希冲突       │   链表           │   链表 + 红黑树           │
│   size计算       │   分段累加       │   CounterCell数组         │
└──────────────────┴──────────────────┴───────────────────────────┘
\`\`\``
  },
  {
    question: 'AQS（AbstractQueuedSynchronizer）是什么？原理？',
    tags: [{ text: '重要', type: 'important' }],
    answer: `## 通俗理解

想象医院挂号：
- **state**：当前还有多少号（资源状态）
- **队列**：排队等待的人（等待线程）
- **AQS**：管理挂号系统的规则（获取/释放资源的框架）

### AQS 结构

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    AQS 核心结构                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              AbstractQueuedSynchronizer                   │   │
│  │                                                          │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  state（int）：资源状态                              │  │   │
│  │  │  - 0：资源可用                                       │  │   │
│  │  │  - 1：资源被占用                                     │  │   │
│  │  │  - N：可重入次数（ReentrantLock）                    │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                          │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  FIFO 队列（CLH变体）：等待线程队列                  │  │   │
│  │  │                                                      │  │   │
│  │  │  head ──→ Node ──→ Node ──→ Node ──→ tail         │  │   │
│  │  │          ThreadA      ThreadB      ThreadC          │  │   │
│  │  │          (等待)       (等待)       (等待)           │  │   │
│  │  │                                                      │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                          │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │  exclusiveOwnerThread：独占模式下的线程             │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### AQS 实现类

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    AQS 实现类                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              AbstractQueuedSynchronizer                   │   │
│  │                      ▲                                    │   │
│  │         ┌────────────┼────────────┐                      │   │
│  │         │            │            │                      │   │
│  │         ▼            ▼            ▼                      │   │
│  │   ┌──────────┐ ┌──────────┐ ┌──────────┐                │   │
│  │   │Reentrant │ │Semaphore │ │CountDown │                │   │
│  │   │   Lock   │ │          │ │  Latch   │                │   │
│  │   └──────────┘ └──────────┘ └──────────┘                │   │
│  │         │                                     ┌────────┐│   │
│  │         │                                     │Cyclic  ││   │
│  │         │                                     │Barrier ││   │
│  │         │                                     └────────┘│   │
│  │         ▼                                               │   │
│  │   ┌──────────┐                                          │   │
│  │   │Reentrant │                                          │   │
│  │   │ReadWrite │                                          │   │
│  │   │   Lock   │                                          │   │
│  │   └──────────┘                                          │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 获取锁流程

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                    获取锁流程                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  线程尝试获取锁：                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  1. tryAcquire() 尝试获取                                 │   │
│  │     ↓                                                    │   │
│  │     成功？ ──→ 获取成功，执行业务                          │   │
│  │     ↓ 失败                                                │   │
│  │                                                          │   │
│  │  2. 加入等待队列                                          │   │
│  │     - 创建 Node 节点                                      │   │
│  │     - CAS 加入队列尾部                                    │   │
│  │     ↓                                                    │   │
│  │                                                          │   │
│  │  3. 自旋或阻塞等待                                        │   │
│  │     - 前驱是头节点？尝试获取锁                            │   │
│  │     - 不是？检查是否需要阻塞（waitStatus）                │   │
│  │     - 需要阻塞？调用 LockSupport.park()                   │   │
│  │     ↓                                                    │   │
│  │                                                          │   │
│  │  4. 被唤醒后继续尝试                                       │   │
│  │     - 前驱释放锁时唤醒后继                                 │   │
│  │     - 回到步骤3                                          │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  释放锁流程：                                                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  1. tryRelease() 释放资源                                 │   │
│  │     - state 减 1                                          │   │
│  │     - state == 0？完全释放                                │   │
│  │     ↓                                                    │   │
│  │                                                          │   │
│  │  2. 唤醒后继节点                                          │   │
│  │     - unparkSuccessor()                                  │   │
│  │     - LockSupport.unpark(后继线程)                        │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\``
  },
  {
    question: '线程池的核心参数？执行流程？拒绝策略？',
    tags: [{ text: '必问', type: 'must' }],
    answer: `## 线程池核心参数

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                  线程池核心参数                                  │
├──────────────────┬──────────────────────────────────────────────┤
│      参数        │                    说明                       │
├──────────────────┼──────────────────────────────────────────────┤
│  corePoolSize    │  核心线程数，常驻线程，即使空闲也保留         │
│  maximumPoolSize │  最大线程数，任务多时可以创建到这么多         │
│  keepAliveTime   │  非核心线程空闲存活时间                        │
│  workQueue       │  任务等待队列                                  │
│  threadFactory   │  线程工厂，创建线程                            │
│  handler         │  拒绝策略，队列满时的处理                      │
└──────────────────┴──────────────────────────────────────────────┘
\`\`\`

### 执行流程

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                  线程池执行流程                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  提交任务                                                        │
│     │                                                           │
│     ▼                                                           │
│  核心线程是否满？ ──→ 否：创建核心线程执行任务                    │
│     │ 是                                                        │
│     ▼                                                           │
│  任务队列是否满？ ──→ 否：加入队列等待                            │
│     │ 是                                                        │
│     ▼                                                           │
│  最大线程是否满？ ──→ 否：创建非核心线程执行任务                  │
│     │ 是                                                        │
│     ▼                                                           │
│  执行拒绝策略                                                    │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │   线程池状态：                                           │   │
│  │                                                          │   │
│  │   ┌─────────┐    ┌─────────┐    ┌─────────┐             │   │
│  │   │核心线程 │    │任务队列 │    │非核心线程│             │   │
│  │   │  2个   │    │  容量10 │    │ 最多3个  │             │   │
│  │   └────┬────┘    └────┬────┘    └────┬────┘             │   │
│  │        │              │              │                   │   │
│  │   常驻执行           排队等待        临时增援              │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
\`\`\`

### 拒绝策略

\`\`\`
┌─────────────────────────────────────────────────────────────────┐
│                  线程池拒绝策略                                  │
├──────────────────┬──────────────────────────────────────────────┤
│      策略        │                    行为                       │
├──────────────────┼──────────────────────────────────────────────┤
│  AbortPolicy     │  默认策略，抛出 RejectedExecutionException   │
│                  │  ┌──────────────────────────────────────┐    │
│                  │  │  throw new RejectedExecutionException │    │
│                  │  │    ("Task " + r.toString() +          │    │
│                  │  │     " rejected from " + e.toString()); │    │
│                  │  └──────────────────────────────────────┘    │
├──────────────────┼──────────────────────────────────────────────┤
│  CallerRunsPolicy│  调用者线程执行任务                           │
│                  │  ┌──────────────────────────────────────┐    │
│                  │  │  if (!e.isShutdown()) {              │    │
│                  │  │      r.run();  // 调用者自己执行      │    │
│                  │  │  }                                   │    │
│                  │  └──────────────────────────────────────┘    │
├──────────────────┼──────────────────────────────────────────────┤
│  DiscardPolicy   │  静默丢弃任务                                 │
│                  │  ┌──────────────────────────────────────┐    │
│                  │  │  // 什么都不做，直接丢弃              │    │
│                  │  └──────────────────────────────────────┘    │
├──────────────────┼──────────────────────────────────────────────┤
│  DiscardOldest   │  丢弃最老的任务，尝试提交新任务               │
│  Policy          │  ┌──────────────────────────────────────┐    │
│                  │  │  e.getQueue().poll();  // 丢弃队首    │    │
│                  │  │  e.execute(r);         // 提交新任务  │    │
│                  │  └──────────────────────────────────────┘    │
└──────────────────┴──────────────────────────────────────────────┘
\`\`\`

### 线程池创建示例

\`\`\`java
// 推荐：使用 ThreadPoolExecutor 手动创建
ThreadPoolExecutor executor = new ThreadPoolExecutor(
    5,                      // 核心线程数
    10,                     // 最大线程数
    60L,                    // 空闲线程存活时间
    TimeUnit.SECONDS,       // 时间单位
    new LinkedBlockingQueue<>(100),  // 任务队列
    new ThreadFactory() {   // 线程工厂
        private final AtomicInteger count = new AtomicInteger(0);
        @Override
        public Thread newThread(Runnable r) {
            return new Thread(r, "my-pool-" + count.incrementAndGet());
        }
    },
    new ThreadPoolExecutor.CallerRunsPolicy()  // 拒绝策略
);

// 不推荐：Executors 的快捷方法（有OOM风险）
// ExecutorService executor = Executors.newFixedThreadPool(10);
// 问题：使用无界队列，任务堆积可能导致OOM
\`\`\``
  },
]
